__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Cache local do token de autenticação (nunca deve ser versionado)
.token_cache.bin

# Sessão salva do navegador (contém cookies de login — nunca versionar)
.browser_state.json

# Saídas dos scripts de diagnóstico: contêm conteúdo real de e-mails
debug_*.png
debug_*.html

# Estado local do app (banco de dados, propostas baixadas)
data/
*.db
.meetings_cache.json

# Saídas da compilação do executável (PyInstaller)
build/
dist/

# Chromium baixado pelo playwright para dentro do projeto na compilação
pw-browsers/

# Perfil do navegador e marcador de login (dados locais do usuário)
navegador/
.conectado
