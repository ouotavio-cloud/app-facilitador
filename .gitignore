__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Saídas dos diagnósticos: contêm conteúdo real de e-mails do usuário —
# nomes de anexo, assunto, remetente, e o HTML do painel de leitura.
# Nunca versionar.
debug_*.png
debug_*.html
diagnostico-anexo.html

# Estado local do app (banco de dados, propostas baixadas)
data/
*.db
.meetings_cache.json

# Saídas da compilação do executável (PyInstaller)
build/
dist/

# Chromium baixado pelo playwright para dentro do projeto na compilação
pw-browsers/

# Perfil do navegador e marcador de login (dados locais do usuário)
navegador/
.conectado
